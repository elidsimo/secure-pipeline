// Placeholder — sera enrichi au Guide 4 avec de vrais tests basés sur
// les abuse cases du threat model (IDOR, injection SQL, rate limiting...)
describe('Security test suite (placeholder)', () => {
  test('le serveur répond sur /health', () => {
    expect(true).toBe(true); // sera remplacé par un vrai test HTTP au Guide 4
  });
});
